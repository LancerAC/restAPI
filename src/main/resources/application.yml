spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    #jdbc:postgresql://localhost:5432/ace
    url: ${DB_URL}
    password: ${DB_PASSWORD}
    username: ${DB_USERNAME}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080